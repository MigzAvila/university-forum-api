services:
  backend:
    image: universityforum-backend-1

    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    network_mode: "host"
    env_file:
      - ./.env
  